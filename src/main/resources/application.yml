server:
  port: 8762
        
## Configuracion clasica de Eureka
eureka:
  instance:
    preferIpAddress: false
    hostname: back-end-cloud-gateway.up.railway.app
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
      

## Habilitamos el service discovery (haciendo uso de Eureka)
## lower-case-service-id necesario ya que por defecto se pone en mayusculas, al cogerlo de Eureka
spring:
  application:
    name: gateway    
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      globalcors: ## Configuracion de CORS a nivel Gateway
        cors-configurations:
          '[/**]': ## Para todas las rutas
            allowedOrigins: '*' ## Si ponemos '*' permitimos todos los origenes. Podemos restringir indicando dominios o ip + puerto, p.ej http://localhost:3000 (donde esta nuestro front)
            allowedMethods:
            - GET
      

## Habilitamos la API Actuator para poder acceder a /actuator/gateway/routes
## Exponemos los routings que se han detectado automáticamente (todos)
management:
  endpoint:
    gateway:  
      enabled: true
  endpoints:
    web:
      exposure:
        include:
        - '*'